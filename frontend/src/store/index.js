/*Store base de Redux Toolkit. Sin slices todavía — los slices se agregan por módulo en la siguiente fase*/
import { configureStore } from '@reduxjs/toolkit';

export const store = configureStore({
  reducer: {},
});