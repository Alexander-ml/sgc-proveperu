# Configuración PostgreSQL

Parámetros de configuración del motor PostgreSQL.

Para entorno local con Docker, la configuración por defecto
de la imagen oficial es suficiente.

Variables críticas ya configuradas en docker-compose.yml:
- TZ=America/Lima
- POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD